// import * as ActionTypes from "../Actions/ActionTypes";
const initialState = {
  username: "",
  email: "",
  password: ""
};

export const UserDetails = (state = initialState, action) => {
  return state;
};
